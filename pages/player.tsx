import type { NextPage } from "next";
import styles from "../styles/index.module.css";
// domain.com/order -> use for order page
const PlayerPage: NextPage = () => {
  return (
    <div>
      <main>THIS IS PLAYER PAGE</main>
    </div>
  );
};

export default PlayerPage;
